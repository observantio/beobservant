global:
  resolve_timeout: 5m

route:
  group_by: ['alertname', 'cluster', 'service']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 12h
  receiver: 'beobservant-webhook'
  routes:
    - match:
        severity: critical
      receiver: 'critical'
      continue: true
    - match:
        severity: warning
      receiver: 'warning'

receivers:
  - name: 'beobservant-webhook'
    webhook_configs:
      - url: 'http://beobservant:4319/alerts/webhook'
        send_resolved: true
        http_config:
          follow_redirects: true

  - name: 'critical'
    webhook_configs:
      - url: 'http://beobservant:4319/alerts/critical'
        send_resolved: true

  - name: 'warning'
    webhook_configs:
      - url: 'http://beobservant:4319/alerts/warning'
        send_resolved: true

inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname', 'instance']